# 회사 PC 인수인계 — 2026-09-20

이 문서가 현재 작업의 출발점이다. 과거 계획 원문은 `HISTORY_LEGACY.md`에 보존했다. 회사 PC에서는 `README.md`의 clone/pull 절차를 따른다.

## 먼저 확인할 실제 상태

| 구분 | 상태 |
| --- | --- |
| 공식 사이트 | https://naengnanmarket.com/ — 9월 20일 Sites 버전 13 게시본. 모바일 전화·문자와 대표 요청 문구 운영 중. 이미지 선로딩·폰트 연결 최적화 반영. **PC 상담 폼은 아직 없음.** |
| GitHub `main` | 9월 20일 PC 상담 폼 코드, 성능 수정, 이 인수인계 문서를 보관. 회사 PC에서 최신 `main`을 받되, GitHub push가 Sites 게시를 뜻하지 않음. |
| PC 상담 폼 | 화면·서버·개인정보 안내를 구현하고 로컬 검사 완료. 메일 활성화와 실제 수신, 사이트 환경값, 공식 배포는 미완료. |
| 네이버 파워링크 | 비즈채널·반응형 소재 등록 화면을 확인했으나 현재 승인 여부는 재조회 필요. 키워드 등록과 송출은 아직 사용자 직접 작업 단계. |

공식 게시 소스는 `4634d23830bf0041bf0f69a37cf37b0808c6fe02`, Sites 버전은 13, 배포 ID는 `appgdep_6aaf98ebd1e0819191e8c9360b7dc770` (`succeeded`)다. 이 소스는 버전 12의 카피·사진·구성을 유지하며 이미지 선로딩과 폰트 CSS 연결만 바꿨다. PC 상담 폼의 첫 로컬 구현 커밋은 `ecca2bafb152613ef8e801c687c62ab2f4096195`다. 이 폼을 포함한 GitHub `main`에 동일한 성능 수정을 별도 커밋으로 반영했으나, 아직 공식 사이트에 게시하지 않았다. 회사에서는 `git log -1`로 최신 SHA를 확인한다.

## 김대곤 대표 요청과 화면

- 상단에 `010-9183-2200`을 누르면 통화되는 링크를 넣고, 첫 화면에 **에어컨 설치**와 **중고 에어컨 판매**를 명시했다. 9월 19일 모바일·PC 공식 화면에서 확인했다.
- 사용자가 정정한 대로 판매를 매장 구매로 설명하지 않는다. 온라인 쇼핑몰·장바구니·결제 계획도 없다. 전화·문자로 제품 상태·가격·설치 조건을 상담한다.
- 모바일 방문자는 전화·문자 직통을 유지한다. PC 방문자는 박성호가 먼저 상담 신청을 받고 김대곤 대표에게 번호를 전달하기로 사용자가 요청했다.
- 기존 서비스인 이전설치·철거·방문 중고 매입·중고 구매 및 설치 안내를 유지한다. 수리·신품 판매, 확인되지 않은 보증·비용·실적을 추가하지 않는다.

## PC 상담 신청 — 구현과 남은 조건

- `app/components/DesktopLeadForm.tsx`, `app/page.tsx`, `app/globals.css`: PC에서만 보이는 신청 폼과 이동 링크. 연락처 필수, 서비스·지역 선택 입력, 개인정보 동의, 접수 결과 안내. 모바일은 기존 전화·문자 동선 유지.
- `app/api/leads/route.ts`: 입력·출처·동의·중복·허니팟 검사 후 기존 D1 `leads`에 저장하고 FormSubmit으로 박성호 수신 메일에 알림. 알림 실패 시 기록은 DB에 남고 화면은 지연 안내와 대표 번호를 표시한다. 기존 인증 관리자 화면에서 수동 확인도 필요하다.
- `app/privacy/page.tsx`: PC 접수, 박성호의 확인과 대표 전달, 외부 메일 알림 처리를 반영. 실제 운영과 안내가 일치하는지 게시 전 확인한다.
- 1440px PC와 390px 모바일 시각 확인, `npm run build`, 자동 검사 5개, 새 컴포넌트·API lint, `git diff --check` 통과. 전체 TypeScript 검사에는 기존 Cloudflare 타입 누락 오류가 있다. 전체 page lint에는 기존 공백 오류가 있다. 새 코드의 오류로 판정하지 않는다.
- `FormSubmit` 첫 인증 요청은 수신 메일로 발송됐으나 **활성화 완료·실제 알림 수신은 확인되지 않았다.** 외부 FormSubmit 사용에 관한 사용자 답변도 아직 확인되지 않았다. `LEAD_NOTIFY_EMAIL` Sites 환경값은 미설정이다. 이메일 주소·비밀번호·관리자 키를 소스에 넣지 않는다.
- 따라서 PC 신청을 공식 사이트에 게시하거나 광고 연결이 완료됐다고 보고하지 않는다. 활성화와 실제 수신이 확인되기 전까지 공식 PC 이용자는 기존 대표 전화·문자로 문의한다.

## 회사에서 이어 할 순서

1. 새 PC에서는 `README.md`대로 GitHub `main`을 받고 `git status`와 `git log -1`을 확인한다. 이미 로컬 변경이 있다면 덮어쓰지 않는다. `npm ci` 후 `npm run dev`로 PC·모바일 화면을 확인한다.
2. 박성호가 수신 메일함과 스팸함의 **FormSubmit Activate Form** 메일을 찾아 활성화한다. 외부 FormSubmit 사용 의사와 개인정보 안내의 실제 처리 방식을 사용자와 확정한다. 이 단계 전에는 고객 정보를 넣은 시험 접수를 하지 않는다.
3. 활성화 후 고객 정보 없는 시험 알림의 실제 메일 도착을 확인한다. Sites에 `LEAD_NOTIFY_EMAIL`을 환경값으로 설정하고 기존 `DB` 바인딩 및 `ADMIN_KEY`를 확인한다. 비밀값은 문서·커밋에 남기지 않는다.
4. `OPERATIONS.md`의 검사와 게시 절차를 따른다. GitHub와 Sites 소스에 같은 검증 소스를 push하고, 같은 SHA로 Sites 버전을 저장·게시한다. 공식 도메인에서 PC 폼과 모바일 전화 버튼을 다시 확인한다.
5. 게시 후 통제된 시험 접수 한 건의 DB 저장·알림 메일·관리자 조회를 확인하고 시험 자료를 운영 기준에 따라 정리한다. 알림 장애 시에는 관리자 화면의 신규 접수를 직접 확인한다.
6. 네이버 비즈채널·소재 승인, 전화번호·이미지 확장 소재의 실제 저장과 승인, 광고계정 권한을 다시 확인한다. 키워드·송출·충전·타인 연락은 사용자가 직접 하기로 한 범위이므로 임의 진행하지 않는다.

## 광고와 운영의 미확인 항목

- 마지막 제공 화면에서 잔액은 10,008원, 캠페인 하루 예산은 10,000원, 광고그룹 기본 입찰은 70원이었으나 현재 값은 아니다. 제목 7개·설명 4개 등록 화면을 확인했고 승인 여부는 재조회해야 한다.
- 키워드는 사용자가 의도적으로 미뤘다. 전화번호 확장 소재와 서브링크 `#` 주소의 실제 저장·승인도 검증되지 않았다. 네이버 전환 추적은 미설치다.
- 사용자 개인 광고계정의 다른 캠페인을 사업자 계정으로 임의 전환하지 않는다. 대표 사업자 계정과 사용자 권한 정리는 논의 단계다.
- 전화·문자 클릭 수는 실제 통화·계약 수가 아니다. GA4는 동의한 방문만 측정한다.
- 미계약 상담의 휴대폰·메일 사본과 DB 기록 삭제 운영, 거래 기록 보존 기준, 중고 보증·교환·환불, 출장·취소 비용, 개인정보 국외 처리 고지의 충분성은 별도 확인이 필요하다. 법률 검증이나 네이버 심사 통과를 보장하지 않는다.

## 보존·배포 주의

- 공식 Sites 프로젝트 ID는 `.openai/hosting.json`의 기존 값을 재사용한다. GitHub Pages `docs/` 출력은 별개이며 GitHub push만으로 공식 Sites 사이트가 바뀌지 않는다.
- `OPERATIONS.md`는 상담 운영과 배포 절차, `PRELAUNCH_CHECKLIST.md`는 광고 전 상태, `PROJECT_BRIEF.md`는 카피·서비스 의도를 설명한다.
- 이 PC에 원래 있던 미추적 `docs/.assetsignore`, `docs/.vite/`, `docs/_headers`, `docs/vinext-client-entry-manifest.json`, `site-publish.tar.gz`는 이번 인수인계에 포함하지 않는다. `git add .`로 섞지 않는다.
