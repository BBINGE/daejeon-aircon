# 작업 인수인계

## 2026-09-13 업데이트 (아래 8월 기록보다 우선)

- 사용자 승인: 도메인 구매 전 기존 사이트 업데이트 및 공개 반영. 광고 집행·도메인 구매는 포함하지 않는다.
- 고객 화면에서 여름·수리 안내 제거, 에어컨·냉난방기의 이전설치·철거·중고 매입·구매 중심으로 개편. 가정·사무실·식당·카페 이전과 겨울 준비를 함께 안내한다.
- 3개 목적 선택에서 문의 폼 유형을 자동 선택. 기존 사진과 디자인을 유지하며 새 철거·반출 사진 2장을 추가한다. 철거 사진은 판매 재고가 아님을 구분한다.
- 등록증 근거 상호·대표·사업자번호·주소 반영. 원본 등록증과 생년월일은 공개하지 않는다.
- 문의 운영은 김대곤 대표 직접 수신으로 변경했다. 기존 ‘박성호 접수 후 대표 전달’ 계획은 폐기한다.
- 미계약 상담 종료 후 3개월 보관: 동의 시각·버전, 종료일·삭제 예정일 추가. 접수/관리자 조회 시 만료 미계약 종료 건 정리. 별도 예약 삭제 작업·자동 문의 알림은 미연결.
- 정책·동의·서비스 이용안내를 현재 기능과 확인된 정보에 맞춰 갱신. 국외 처리·위탁 세부 정보와 영업 정책은 아직 광고 전 확인 대상이다. ‘광고 준비 완결’로 보고하지 않는다.
- 운영 및 검증 절차는 OPERATIONS.md 참고. 배포 결과는 최종 확인 기록을 따른다.

최종 업데이트: 2026-08-10

## 현재 상태

- 반응형 랜딩페이지 1차 UI 완성
- 실제 현장 사진 8장을 WebP로 최적화해 적용
- PC 1440px, 모바일 390px 화면에서 가로 넘침 여부 검수
- Pretendard 웹폰트 적용 및 플랫폼형 UI 1차 정리
- 템플릿형 나열보다 정보 위계, 통일된 간격, 신뢰 중심 컴포넌트 체계를 우선
- Hero 한글 음절 중간 줄바꿈을 제거하고 의도된 3행 제목으로 고정
- CTA 텍스트와 화살표를 중앙 그룹으로 정렬하고 hover/focus 상호작용 체계 추가
- 영업형 서비스 나열보다 실생활 상황 중심의 `이럴 때 신청하세요` 섹션 추가
- 이전·신규설치, 수리, 중고 구매의 구체적인 신청 상황과 가정집·기업·사무실·매장 상담 메시지 반영
- IntersectionObserver 기반 스크롤 등장 효과, 상단 진행선, 데스크톱 Hero 약한 패럴랙스 추가
- 모바일 및 `prefers-reduced-motion` 환경에서는 스크롤 효과를 축소하거나 비활성화
- `/privacy`, `/terms` 사전 오픈용 초안 페이지와 실제 이동 링크 추가
- 사업자·전화·주소·DB 운영 정보는 허위값 대신 `[대표 확인 필요]`로 표시
- `PRELAUNCH_CHECKLIST.md`에 김대곤 대표 통화 확인 항목과 버튼 이동표 작성
- PC 1440/1024, 태블릿 768, 모바일 390/360 폭 가독성 검수
- 1024px Hero 폼 제목의 한글 음절 분리와 선택창 답답함을 중간 breakpoint로 보정
- 정책 페이지 모바일 상단 링크의 터치 영역을 44px 이상으로 확대
- 개인정보·서비스 안내 표를 모바일에서 가로 스크롤 대신 항목별 세로 카드로 전환
- 실제 작업 섹션을 7장 연속 자동 슬라이드 갤러리로 교체
- PC는 hover 시 일시정지, 모바일은 자동재생 없이 직접 스와이프, 모션 감소 환경은 자동 이동 중지
- 대표 시연용으로 PC·모바일 스크롤 등장 효과를 확대: blur 해제, 이동, scale, 카드 stagger 적용
- Hero 하단에 움직이는 스크롤 안내를 추가하고 90px 이상 스크롤 시 자동 숨김
- 모바일 좌우 진입 애니메이션 중 발생하던 9px 임시 가로 영역을 clip 처리
- Cloudflare D1 `DB` 바인딩과 `leads` 테이블 추가
- `/api/leads` POST 전용 API에서 서버 검증 후 문의 DB 저장
- 지역·문의유형·에어컨종류·연락처·신청시각·유입 URL·referrer·UTM 값을 저장하고 초기 상태는 `new`
- Sites 배포본에서는 문의 폼과 Cloudflare D1 `leads` 저장이 연결됨
- 공개 GitHub 저장소 `BBINGE/daejeon-aircon` 생성 및 `main` 브랜치 푸시
- GitHub Pages 공개 주소: `https://bbinge.github.io/daejeon-aircon/`
- GitHub Pages는 `docs/` 정적 배포본을 사용하며 PC 1280px·모바일 390px 공개 화면 검수 완료
- GitHub Pages 고객 화면은 공개 Sites 백엔드 `https://kimdaegon-aircon.bbinge95.chatgpt.site/api/leads`에 연결되어 실제 D1 DB에 저장됨
- Sites 백엔드는 공개 접근 상태지만 POST 접수만 인증 없이 허용하며, DB 조회·상태 변경 GET/PATCH는 `ADMIN_KEY`로 보호
- PC 우측 고정 퀵 네비, 모바일 하단 2행 CTA, 상단 프로모션 배너 추가
- 실제 현장 섹션 제목을 문장 단위 4행으로 고정해 어색한 한글 줄바꿈 제거
- 김대곤 대표 상담 전화 `010-9183-2200` 확인 및 PC·모바일 전화 CTA와 정책 페이지에 `tel:01091832200` 연결
- 플랫폼형 블루 라운드 스퀘어와 냉기 흐름을 결합한 SVG 파비콘 제작 및 메타데이터 연결
- GitHub Pages 폼의 API 목적지를 Sites 공개 백엔드 `/api/leads`로 분리
- `/api/leads`에 GitHub Pages CORS, 봇 허니팟, 2분 중복 접수 방지, 관리자 키 인증 GET/PATCH 추가
- GitHub Pages `/admin.html`에 문의 목록, 검색, 상태 변경, 전화 연결, CSV 다운로드 관리자 화면 추가
- 관리자 화면의 React hydration 오류를 제거하고 독립형 정적 페이지로 교체해 로그인·조회·상태 변경 동작 확인
- 관리자 키는 GitHub나 코드가 아닌 Sites 비밀 환경 변수 `ADMIN_KEY`에만 저장
- 고객 PC·모바일 주소: `https://bbinge.github.io/daejeon-aircon/`
- DB 관리자 주소: `https://bbinge.github.io/daejeon-aircon/admin.html`
- Sites 프로젝트 ID는 `.openai/hosting.json`에 저장되어 있으며 배포와 D1 연결에 재사용
- 실제 접수→D1 저장→관리자 인증 조회→상태 변경까지 운영 환경에서 검증 완료
- 개인정보 전문, 사업자 정보, 대표 연락처는 미확정 상태
- 문의 운영 담당자는 박성호이며, 신규 문의를 먼저 확인한 뒤 김대곤 대표에게 실시간 전달하는 방식으로 운영 예정
- 신규 문의 알림 수신 예정 이메일은 `sho3603@naver.com`
- 이메일 자동 알림은 아직 연결되지 않음. 도메인·메일 발송 계정이 없는 현재 단계에서는 FormSubmit 같은 외부 발송 서비스를 사용할 수 있으나 고객 연락처가 외부 서비스를 거치므로 개인정보 처리 고지 및 사용자 승인을 받은 뒤 연결해야 함
- 스팸·오류 보강 예정 항목: IP별 접수 제한, 단시간 대량 요청 차단, 서버 오류 기록, 이메일 발송 실패 시에도 DB 접수 유지

## 1차 카피 방향

메인 문구: `더위는 이미 왔습니다. 에어컨 일정은 기다려주지 않습니다.`

주 CTA: `지금 가능한 일정 확인하기`

핵심 광고 서비스는 `에어컨 수리 / 에어컨 설치 / 에어컨 이전설치 / 중고 에어컨 판매` 네 가지를 독립적으로 노출한다. 중고 매입, 철거, 가스 충전은 보조 서비스로 유지한다.

## 다음 작업

1. 대표 전화번호, 상호, 사업자 정보 확인
2. 개인정보 수집 및 운영 구조 최종 확정
3. `sho3603@naver.com` 신규 문의 이메일 알림 방식 확정 및 연결
4. IP별 접수 제한·대량 요청 차단·오류 기록 보강
5. GA4 전환 이벤트 및 네이버 광고 추적 연결
6. 테스트 문의 2건 정리 및 관리자 삭제 기능 필요 여부 결정
7. 도메인 구매 후 GitHub Pages 사용자 도메인 연결

## 현재 보고 가능 수준

- 김대곤 대표에게 PC·모바일 랜딩페이지, 실제 문의 접수, DB 관리자 화면을 시연할 수 있음
- 디자인·서비스 구성·전화 CTA·현장 사진·문의 저장·관리자 조회까지는 보고 가능
- 아직 `광고 즉시 집행 완료` 상태로 보고하면 안 됨
- 광고 오픈 전 필수 확인: 실제 상호·사업자등록번호·주소·개인정보 운영 내용 확정, 이메일 알림 또는 상시 DB 확인 방식 확정
- 대표 보고 시에는 `사이트와 DB 접수는 완성됐고, 대표 정보 입력과 알림 연결 후 광고를 켤 수 있다`고 설명하는 것이 정확함

## 다른 PC에서 이어가기

1. 공개 저장소 `https://github.com/BBINGE/daejeon-aircon`을 clone 또는 pull한다.
2. 새 작업을 시작하기 전에 이 `HANDOFF.md`와 `PRELAUNCH_CHECKLIST.md`를 읽는다.
3. 공개 고객 화면은 GitHub Pages, 문의 저장·조회 API와 D1은 Sites가 담당하는 분리 구조를 유지한다.
4. 관리자 비밀번호는 저장소에 없으므로 사용자에게 별도로 전달받는다. 코드나 커밋에 추가하지 않는다.
5. 원본 사진 폴더는 Git에 포함되지 않는다. 웹에 쓰는 최적화 이미지와 배포 파일은 저장소에 포함되어 있다.

## 주의

- 원본 사진 폴더는 용량이 크므로 그대로 Git에 커밋하지 않는다.
- 공개 저장소에 비밀번호, 계좌번호, 고객 연락처를 올리지 않는다.
- 작업 지역과 일정은 접수 후 확인하는 구조를 유지한다.
